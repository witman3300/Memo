# ☁ 구글 드라이브 동기화 설정

PC 위젯과 휴대폰 웹앱이 **내 구글 드라이브의 `MemoWidget/content.txt` 한 파일**을 같이 씁니다.
메모 내용은 내 드라이브에만 저장되고, 다른 서버를 거치지 않습니다.

```
[PC 위젯]  ──┐
             ├──→  내 구글 드라이브 : MemoWidget/content.txt
[폰 웹앱]  ──┘
```

전체 30분이면 끝나고, **한 번만** 하면 됩니다.

---

## 1단계 — 구글 클라우드 프로젝트 만들기 (5분)

1. <https://console.cloud.google.com/> 접속 → 상단 프로젝트 선택 → **새 프로젝트**
   - 이름: `MemoWidget` (아무거나)
2. **API 및 서비스 → 라이브러리** → `Google Drive API` 검색 → **사용 설정**
3. **API 및 서비스 → OAuth 동의 화면**
   - User Type: **외부** → 만들기
   - 앱 이름 `메모위젯`, 사용자 지원 이메일 / 개발자 연락처에 본인 이메일
   - 범위(scope)는 건너뛰어도 됩니다
   - **테스트 사용자**에 **본인 구글 계정 이메일을 반드시 추가**
   - 저장

> 앱을 "게시"하지 않고 테스트 모드로 두면 됩니다. 본인 계정만 쓰는 앱이라 심사가 필요 없습니다.
> 테스트 모드에서는 데스크톱 리프레시 토큰이 **7일마다 만료**될 수 있습니다 —
> 위젯의 ☁ 아이콘이 빨갛게 되면 우클릭 → [연결하기]를 다시 누르면 됩니다.
> 매번 하기 번거로우면 OAuth 동의 화면에서 **앱 게시(프로덕션으로 push)** 를 눌러두세요.
> `drive.file` 범위는 민감 범위가 아니라서 별도 심사 없이 게시됩니다.

---

## 2단계 — PC 위젯용 자격증명 (데스크톱 앱)

1. **API 및 서비스 → 사용자 인증 정보 → + 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
2. 애플리케이션 유형: **데스크톱 앱** → 이름 `MemoWidget Desktop` → 만들기
3. 나온 **클라이언트 ID**와 **클라이언트 보안 비밀**을 복사
4. 메모 위젯 실행 → 상단 **☁ 아이콘 클릭**(또는 우클릭) → 설정 창이 열림
5. 복사한 ID/시크릿을 붙여넣고 **[연결하기]** → 브라우저에서 구글 로그인 → 허용

> "Google에서 확인하지 않은 앱" 경고가 뜨면 **고급 → (앱 이름)(으)로 이동** 을 누르면 됩니다.
> 내가 만든 내 앱이라 정상입니다.

연결되면 ☁ 아이콘이 초록색이 되고 **1분마다 자동 동기화**됩니다.
클릭하면 즉시 동기화, 우클릭하면 설정 창이 열립니다.

자격증명과 토큰은 저장소가 아니라 `%APPDATA%\desktop-memo\google.json` 에만 저장됩니다.

---

## 3단계 — 폰 웹앱 호스팅 (GitHub Pages)

구글 드라이브는 2016년에 웹페이지 호스팅 기능이 폐지돼서, 웹앱 자체는 다른 곳에 올려야 합니다.
이미 이 프로젝트가 GitHub에 있으므로 **GitHub Pages**가 가장 간단합니다 (무료).

1. 이 저장소를 GitHub에 푸시
   ```bash
   git add -A
   git commit -m "구글 드라이브 동기화 + 모바일 웹앱"
   git push
   ```
2. GitHub 저장소 → **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: `main` / 폴더 `/ (root)` → Save
3. 1~2분 뒤 주소가 살아납니다:
   **<https://witman3300.github.io/Memo/web/>**

---

## 4단계 — 폰 웹앱용 자격증명 (웹 애플리케이션)

1. **사용자 인증 정보 → + 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
2. 애플리케이션 유형: **웹 애플리케이션** → 이름 `MemoWidget Web`
3. **승인된 자바스크립트 원본**에 추가:
   ```
   https://witman3300.github.io
   ```
   (로컬에서 테스트하려면 `http://localhost:8123` 도 함께 추가)
4. 만들기 → **클라이언트 ID** 복사 (여기서는 시크릿을 쓰지 않습니다)
5. `web/config.js` 를 열어 붙여넣기:
   ```js
   window.MEMO_CONFIG = {
     CLIENT_ID: '여기에-붙여넣기.apps.googleusercontent.com',
     AUTO_SYNC_MS: 45000
   };
   ```
6. 커밋 & 푸시 → GitHub Pages가 자동 갱신

> 웹 클라이언트 ID는 공개돼도 되는 값입니다. **시크릿은 절대 여기에 넣지 마세요.**

---

## 5단계 — 폰 홈화면에 앱으로 설치

1. 폰 브라우저로 <https://witman3300.github.io/Memo/web/> 접속
2. **[구글 계정으로 연결]** → PC와 **같은 구글 계정**으로 로그인 → 허용
3. 홈화면에 추가
   - **Android(Chrome)**: 우상단 ⋮ → **홈 화면에 추가** / **앱 설치**
   - **iPhone(Safari)**: 공유 버튼 → **홈 화면에 추가**

이제 홈화면 아이콘으로 앱처럼 열리고, 주소창 없이 전체화면으로 뜹니다.

---

## 쓰는 법 (폰)

| 동작 | 방법 |
|---|---|
| 항목 완료 토글 | 체크박스 **탭** |
| 편집 | 우상단 **✎** → 고치고 **완료** |
| 번호 정리 | **↻** (빈 줄·`빈출` 줄 정리, 번호 다시 매김) |
| 되돌리기 | **↶** |
| 지금 동기화 | **☁** |

- 편집을 마치면 2.5초 뒤 자동으로 드라이브에 올라갑니다.
- 앱을 다시 열 때 / 45초마다 드라이브를 확인해 최신 내용을 가져옵니다.
- **오프라인에서도** 메모를 보고 고칠 수 있고, 온라인이 되면 자동으로 맞춰집니다.

---

## 충돌은 어떻게 처리되나요

PC와 폰에서 **동시에** 다른 내용을 고쳤다면, 나중에 동기화한 쪽이 본문이 되고
상대편 내용은 드라이브의 `MemoWidget/content-conflict-20260829-1430.txt` 처럼
**시각이 붙은 파일로 백업**됩니다. 어느 쪽 내용도 사라지지 않습니다.

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| ☁ 가 빨간색 | 아이콘에 마우스를 올려 오류 메시지 확인. `invalid_grant` 면 우클릭 → 다시 [연결하기] |
| 폰에서 "연결 실패" | 승인된 자바스크립트 원본에 `https://witman3300.github.io` 가 정확히 들어갔는지 확인 (끝에 `/` 없이) |
| "확인되지 않은 앱" 경고 | 고급 → 앱으로 이동. OAuth 동의 화면 테스트 사용자에 내 계정이 있어야 함 |
| 폰과 PC 메모가 다름 | 양쪽에서 ☁ 를 한 번씩 눌러 강제 동기화 |
| 연결을 끊고 싶음 | 위젯 ☁ 우클릭 → [연결 해제] / <https://myaccount.google.com/permissions> 에서도 해제 가능 |

## 이 앱이 접근하는 범위

`https://www.googleapis.com/auth/drive.file` — **이 앱이 직접 만든 파일만** 읽고 씁니다.
드라이브의 나머지 파일에는 접근할 수 없습니다.
